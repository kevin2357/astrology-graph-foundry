# Chunk 2.8 Repository Separation

Projection ownership moved to the independent **Semantic Projection Core** repository/package (`semantic_projection`). Astrology Graph Foundry retains `projection_adapter.py`, the CLI bridge, source-registry adaptation, temporal-package rejection, and integration QA.

The repository display identity is now Astrology Graph Foundry; the Python import package remains `astrology_graph_foundry` pending a separate focused namespace-renaming pass.
