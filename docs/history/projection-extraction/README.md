# Projection extraction history

Projection was initially developed inside Foundry and later extracted into Semantic Projection Core. This directory retains selected historical material from that period.

Current projection contracts, profiles, contexts, materialization behavior, temporal execution, and Synastry preparation are documented and implemented in the Semantic Projection Core repository. Git history preserves removed migration pointers and filename-corrupted duplicate copies that had no continuing documentary value.
